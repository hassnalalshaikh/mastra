# Chat repair scope

The Khayalek owner requested all reported chat defects repaired and proved on 2026-09-14, after the gateway-loss contribution was described. This authorizes a narrowly scoped extension of KV-AG-004, preserving every contribution in source d268b986024359f7609ed75d08cca0ed3906f145.

Responsibility: retain the selected model gateway across durable serialization and reconstruction. Target: the long-running Mastra server and its durable agent. The browser has no execution or recovery authority. Door D1 retains native Session ownership; door D5 retains the existing commercial route validation.

Both required skills were read. Official docs MCP 1.2.25 is live: local-package discovery and remote Session reference succeeded. Exact installed Core and Server are 1.63.2, client-js 1.42.4, at d268b986. Official stable Core 1.66.0 contains the same serialize-state defect. Installed public durable serializeModelConfig and ModelRouterLanguageModel reproduce the gateway change without a provider call. Both gateway-prefixed identifiers reported by the owner lose their first segment and reconstruct through models.dev. The current official Session and memory references were also inspected; no product routing alias is needed or allowed.

Decision: KNOWN_ALLOWED_VIOLATION:KV-AG-004, exact hash-pinned upstream contribution artifact. Preserve only the original routing identifier, never credential-bearing model configuration. The complete native path remains AgentController Session -> durable agent -> serialized workflow -> registered Mastra gateway. No product lifecycle or billing state writes. Removing the contribution restores stock serialization, including its demonstrated defect; it is temporary contribution code and is not claimed to be stock native behavior.

Acceptance: regression first; exact source tests and compilers; published archive identity; the real local long-running server path. A local server receipt does not prove deployed container recovery. No provider calls are needed for serialization proof. External publication is restricted to this task's source branch and immutable package artifacts. Stop on any changed gateway, secret in the snapshot, lost prior contribution, or package identity mismatch.

Responsibility: edit a saved user message without deleting the original conversation. The owner explicitly requested this repair and instructed completion after the absence of a complete native edit path was reported. The chosen behavior is an edited copy. This extends the temporary contribution scope only for a Controller-owned edit command and its authenticated Server/client exposure. Installed Session, Memory.cloneThread, client and server contracts, current official Session reference, current Studio message handling and Core 1.66.0 were inspected. Full memory clones copy later observations, so the edit uses the native memory storage clone for the exact earlier message IDs; it starts a fresh thread summary. Shared resource memory remains shared, as it does for a new chat. Attachments are read through native signal conversion. No original messages, billing records, or tool effects are deleted or replayed. The native Controller creates the target session and submits the replacement through its existing signal acceptance path. The browser supplies intent and navigates to the returned thread only. An edited-copy creation failure must leave the original untouched; a rejected send keeps the copy available for recovery. Local tests must cover ownership, first-message editing, large history, files, copy failure, acceptance failure and duplicate targets. No paid calls during this repair.

Failure proof found that plain edit validation errors became HTTP 500 and triggered four client attempts. Reuse the existing public MastraError status convention already used by native schedules. Invalid input is 400, missing or foreign messages are 404, and duplicate targets are 409; unexpected storage failures remain 500. Server mapping and native client no-retry behavior are tested. This adds no product retry or error translation layer.
